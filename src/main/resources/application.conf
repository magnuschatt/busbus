ktor {
    deployment {
        port = 9000
        environment = development
        autoreload = true
        watch = [ busbus ]
    }

    application {
        id = busbus
        modules = [ chatt.busbus.MainKt.main ]
    }
}
